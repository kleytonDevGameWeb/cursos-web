const cors = ['white', 'orange', 'blue', 'yellow', 'black', 'red', 'purple', 'green'];
const players = [
  {x: 50, y: 50, w: 50, h: 50, c: 'white'},
  {x: 140, y: 90, w: 100, h: 100, c: 'red'}
]

//cors.sort();
//cors.reverse();

//let x = cors.toSorted();
let x1 = cors.toReversed();

console.log(x1);