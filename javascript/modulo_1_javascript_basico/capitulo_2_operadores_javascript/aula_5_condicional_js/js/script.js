// instrução if, else if, else
let condition1, condition2 = true;
if (condition1) {
  //code
} else if (condition2) {
  //code
} else {
  //code
}

// instrução switch
let expression = false;
let x, y = 0;
switch(expression) {
  case x:
    //code
    break;
  case y:
    //code
    break;
  default:
    //code
}

// Operador ternário (? :)
let x1 = 7 > 9 ? "yes" : "no";
console.log(x1);